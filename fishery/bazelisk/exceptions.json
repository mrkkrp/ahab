{
  "exceptions": [
    {
      "reason": "bazelisk links Go with Bazel's autoconfigured non-hermetic C++ toolchain, which puts the host compiler's directory on PATH",
      "kind": "bad_path"
    },
    {
      "reason": "bazelisk links Go with Bazel's autoconfigured non-hermetic C++ toolchain, named by absolute path",
      "kind": "absolute_path",
      "path": "*/bin/gcc"
    },
    {
      "reason": "bazelisk links Go with Bazel's autoconfigured non-hermetic C++ toolchain, named by absolute path",
      "kind": "absolute_path",
      "path": "*/bin/ar"
    }
  ]
}
